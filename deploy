rsync -av ./ julesitinr@poivron.o2switch.net:~/cr-an-search --exclude="/venv" --exclude=".idea/"

# ssh julesitinr@poivron.o2switch.net

# cd chaudoudoux-mdl.fr

# composer install --no-dev --optimize-autoloader

# APP_ENV=prod APP_DEBUG=0 php bin/console cache:clear